# PRICING_SPEC_V2.md — нове ціноутворення та відображення цін

---

## 1. Оригінальний запит (без змін)

> Іде до блоку — ціноутворення
>
> Ціноутворення та відображення.
>
> 1. Прогер підготував зміни. Тепер буде ось така інфа надходить
>
> https://eclub.com.ua/api/v1/json/routes/ При пошуку поїздки повертає в тому числі:
> - price_old - базова ціна
> - price_alt - альтернативна ціна на поїздку, 0 - якщо немає альтернативної ціни
> - price_dsc - розмір знижки на поїздку у відсотках, 0 - якщо знижки немає
> - price_api_dsc - розмір знижки на поїздку для API у відсотках, 0 - якщо знижки немає
> - price_mob_dsc - розмір знижки на поїздку для застосунку у відсотках, 0 - якщо знижки немає
>
> Так, і тепер появляються які нюанси.
>
> Записуй правила. це дуже важливо:
>
> **Для поїздок в 1 сторону**
>
> 1. Потрібно показувати базову ціну завжди - як ціну тарифу, але є одне виключення.
> Якщо альтернативна ціна price_alt більша ніж базова ціна price_old то в таких випадках
> альтернативна ціна стає базовою.
> 2. Якщо всі значення окрім базової ціни = 0 то виводимо тільки одну ціну на екран (базову
> або як у винятку написаному в пункті 1)
> 3. Якщо в масиві є значення меньші ніж базова ціна, то міняємо відображення. Базову ціну
> робимо перекресленою і показуємо нову меньшу ціну. і під нею пишемо знижка на рейсі 10%
> (наприклад) - показуємо реальний відсоток знижки.
>
> **Для поїздок в 2 сторони - з двома фіксованими датами або зворотною вільною**
>
> Відходимо від таблиць, які були раніше. Зараз будемо самі рахувати вартість квитка по
> формулі. Число базового тарифу показуємо так:
>
> 1. (Базовий тариф поїздки1 + базовий тариф поїздки 2) помножений на коефіцієнт - 0,95
> (ось це виключення працює і тут: Якщо альтернативна ціна price_alt більша ніж базова
> ціна price_old то в таких випадках альтернативна ціна стає базовою.)
>
> Зробити в панелі управління додатком там де курс - можливість міняти цей коефіцієнт:
> загально і створювати правила для обраних міст. Тобто менеджер зможе включити міста на
> своїй лінії окремі для маркетингових цілей і там знизити ціну, або при інтенсивній
> загрузці - включити більшу ціну з конкретного міста)
>
> 2. Якщо всі значення поїздки 1 та поїздки 2 окрім базової ціни = 0, то виводимо тільки
> одну ціну - називаємо його для себе - "Базовий тариф раунд тріп"
> 3. Якщо є значення хоча б в одній поїздці меньше в масиві то ми беремо його у формулу.
> І такий тариф називаємо - актуальний тариф раунд тріп
> 4. на рейсах з відкритою датою коефіцієнт беремо не 0,95 а 0,9 але залишаємо можливість
> його змінити. і на рейсах з відкритою датою беремо таку формулу: поїздка 1 (базовий або
> актуальний тариф, актуальний замінює базовий) + базовий тариф зворотної поїздки за цим
> маршрутом на 30+ днів вперед від дати виїзду і суму цих тарифів множимо на коефіцієнт 0,9
> 5. Тепер давай врахуємо, що в нас знижки не сумуються, тому якщо пасажир обирає собі
> фіксовну знижку з нашого переліку ми змінюємо формулу так: ціна поїздки 1 з знижкою +
> ціна поїздки 2 з тією ж знижкою. і їх суму множимо на коефіцієнт. для поїздки з вільною
> датою за правило зворотну ціну беремо на рейсі що буде через 30 днів після першого рейсу
> але на зворотний маршрут. якщо точного збігу немає зворотного рейсу то найближчий вперед.
>
> Тобто потрібно додати нове ціноутворення і нове відображення цін візуально. на етапі
> сторінки бронювання ми відображаємо вже тільки одну ціну а от в інфо про замовлення і в
> квитку виводимо - повний тариф або актуальний тариф і ціна. Де ціна - це сума до сплати
> зі знижками, якщо такі є. якщо ціни однакові то не виводимо 2 одночасно
>
> Серед знижок ми використовуємо тільки ці:
> - price_dsc — загальна знижка на поїздку, % (0, якщо немає)
> - price_mob_dsc — знижка спеціально для застосунку, % (0, якщо немає)
>
> Цю можеш відразу забути і ігнорувати. нам вона не буде потрібна:
> - price_api_dsc
>
> І по пріоритетах, то ця знижка має вищий пріоритет:
> - price_mob_dsc — знижка спеціально для застосунку, % (0, якщо немає)
>
> чим загальна, бо вона зроблена спеціально під застосунок, для маркетингових цілей.

---

## 2. Професійна специфікація

### 2.1 Довідник полів і термінів

| Назва | Джерело | Опис |
|---|---|---|
| `price_old` | бекенд, `routes` | Базова ціна поїздки (одного напрямку), як є на сервері |
| `price_alt` | бекенд, `routes` | Альтернативна ціна. `0` = немає альтернативної ціни |
| `price_dsc` | бекенд, `routes` | Загальна знижка на поїздку, % (`0` = немає) |
| `price_api_dsc` | бекенд, `routes` | Знижка для API-каналу, % — **ІГНОРУЄТЬСЯ повністю, не використовується ніде** |
| `price_mob_dsc` | бекенд, `routes` | Знижка спеціально для застосунку, % (`0` = немає). **Пріоритетна** над `price_dsc` |
| **Базовий тариф (leg)** | обчислюється | `price_alt > price_old ? price_alt : price_old` — застосовується до **кожної** поїздки (leg) окремо, скрізь, де фігурує "базовий тариф" |
| **Обраний % знижки (leg)** | обчислюється | `price_mob_dsc > 0 ? price_mob_dsc : (price_dsc > 0 ? price_dsc : 0)` — `price_api_dsc` в цій формулі не бере участі |
| **Актуальна ціна (leg)** | обчислюється | `Базовий тариф (leg) × (1 − Обраний % знижки (leg) / 100)` |
| **Базовий тариф раунд тріп** | обчислюється | `(Базовий тариф leg1 + Базовий тариф leg2) × коефіцієнт` — використовується, коли жодна з поїздок не має знижки/альтернативи, що зменшує ціну |
| **Актуальний тариф раунд тріп** | обчислюється | Те саме, але з підстановкою **Актуальної ціни (leg)** замість Базового тарифу для тієї поїздки (чи обох поїздок), де є знижка |
| **Коефіцієнт (round-trip)** | налаштування адмінки | За замовчуванням `0.95` для рейсів із двома фіксованими датами; `0.9` для рейсів із відкритою датою повернення. Редагується в адмінці — глобально і по обраних містах |

### 2.2 Односторонні поїздки (one-way)

**Крок 1 — визначити базовий тариф:**
```
базовийТариф = price_alt > price_old ? price_alt : price_old
```

**Крок 2 — визначити обраний % знижки:**
```
знижкаПроц = price_mob_dsc > 0 ? price_mob_dsc : (price_dsc > 0 ? price_dsc : 0)
// price_api_dsc ІГНОРУЄТЬСЯ
```

**Крок 3 — відображення:**
- Якщо `знижкаПроц === 0` (немає жодної застосовної знижки) → показуємо **ОДНУ** ціну — `базовийТариф` (він же вже враховує виняток `price_alt`)
- Якщо `знижкаПроц > 0` → показуємо:
  - `базовийТариф` — **перекреслено**
  - нижче: **актуальна ціна** = `базовийТариф × (1 − знижкаПроц / 100)`
  - під нею: **"Знижка на рейсі {знижкаПроц}%"** — реальний відсоток

*(Пункти 2 і 3 з оригінального промту логічно об'єднуються в одну умову: "знижкаПроц === 0" vs "знижкаПроц > 0" — не потрібні окремі перевірки "чи всі поля нульові".)*

### 2.3 Поїздки в два боки (round-trip: фіксовані дати АБО відкрита дата повернення)

Повністю відходимо від старих табличних тарифів twoWayGroup — рахуємо динамічно за формулою.

**Крок 1 — для КОЖНОЇ поїздки (leg1, leg2) окремо обчислити:**
- Базовий тариф (leg) — за тим самим правилом `price_alt > price_old ? price_alt : price_old`
- Обраний % знижки (leg) — `price_mob_dsc` пріоритетно, інакше `price_dsc`
- Актуальна ціна (leg) — `Базовий тариф (leg) × (1 − % / 100)`, якщо знижка є

**Крок 2 — режим "фіксовані дати в обидва боки":**

```
якщо на ОБОХ leg1 і leg2 знижкаПроц === 0:
    Базовий тариф раунд тріп = (БазовийТариф_leg1 + БазовийТариф_leg2) × коефіцієнт(0.95)
    → показуємо ОДНУ ціну (Базовий тариф раунд тріп)

інакше (хоча б на одній з поїздок є знижка):
    Актуальний тариф раунд тріп =
        (АктуальнаЦіна_leg1_або_БазовийТариф_якщоНемаєЗнижки +
         АктуальнаЦіна_leg2_або_БазовийТариф_якщоНемаєЗнижки) × коефіцієнт(0.95)
    → показуємо: Базовий тариф раунд тріп (перекреслено) + Актуальний тариф раунд тріп
```

**Крок 3 — режим "відкрита дата повернення":**

Коефіцієнт за замовчуванням — **0.9** (не 0.95), редагований так само в адмінці.

```
базовийТарифЗворотного = Базовий тариф маршруту (той самий напрямок туди-назад)
                          на рейс, що є 30+ днів ВПЕРЕД від дати виїзду leg1
                          (якщо точного рейсу на 30-й день немає — беремо
                           найближчий рейс ПІСЛЯ цієї точки, не раніше)

Актуальний тариф раунд тріп (відкрита дата) =
    (АктуальнаЦіна_leg1_або_БазовийТариф_якщоНемаєЗнижки + базовийТарифЗворотного) × коефіцієнт(0.9)
```

**Крок 4 — знижки не сумуються (застосовується і до фіксованих дат, і до відкритої):**

Якщо пасажир обирає **фіксовану знижкову категорію** (з нашого переліку — за повним тарифом/дитяча/УБД тощо, не `price_dsc`/`price_mob_dsc`):

```
ціна leg1 з цією категорією-знижкою + ціна leg2 з ТІЄЮ Ж категорією-знижкою,
сума × коефіцієнт

для відкритої дати: замість ціни leg2 (фактичного рейсу) — та сама категорія-знижка,
застосована до зворотного тарифу (правило 30+ днів вперед, найближчий рейс якщо точного
збігу нема)
```

*Ключове правило: `price_mob_dsc`/`price_dsc` (автоматичні знижки бекенду) і фіксовані категорії-знижки пасажира — **взаємовиключні**, ніколи не застосовуються одночасно одна на одну.*

### 2.4 Відображення на різних екранах

| Екран | Що показуємо |
|---|---|
| **Сторінка результатів пошуку** (списки рейсів) | Правила з 2.2/2.3 — одна ціна, або перекреслена+нова+% знижки |
| **Сторінка бронювання (Booking)** | **ОДНА** ціна — фінальна сума до сплати, без перекреслень і без показу "було/стало" |
| **Деталі замовлення / квиток** | "Повний тариф" **або** "Актуальний тариф" (те, що застосовне за 2.2/2.3) **та** "Ціна" (= сума до сплати з урахуванням знижок). **Якщо ці два числа однакові — показуємо тільки ОДНЕ**, не дублюємо |

### 2.5 Налаштування коефіцієнта в адмінці (нова фіча, для реалізації)

Розділ поруч із курсом валют:
- **Глобальний коефіцієнт** — окремо для "фіксовані дати" (за замовч. 0.95) і "відкрита дата" (за замовч. 0.9)
- **Правила по містах** — менеджер може задати **інший** коефіцієнт для конкретного міста відправлення (маркетингове зниження ціни на певному напрямку, або підняття при високому завантаженні)
- Пріоритет: правило по місту (якщо є) → інакше глобальне значення

---

## 3. Питання, які варто уточнити в прогера перед реалізацією

1. **`routes_day`** (пошук по днях/календар) — чи ці ж самі поля (`price_old`, `price_alt`, `price_dsc`, `price_mob_dsc`) повертаються і тут, чи лише в основному `routes`? Якщо календар показує ціни на кожен день — знадобиться і там.
2. **Формат `price_alt`** — чи може `price_alt` бути **меншим**, ніж `price_old` (тобто теж діяти як знижка, не тільки "підвищена альтернативна")? У специфікації описано лише випадок `price_alt > price_old`. Якщо `price_alt < price_old` теж можливий — як його трактувати (як ще одну знижку? ігнорувати?).
3. **`price_mob_dsc` для двосторонніх** — застосовується per-leg (окремо на кожен напрямок, як описано вище) чи є **єдине** значення на весь round-trip? Поточна специфікація враховує per-leg.
4. **Округлення** — до яких знаків округляти фінальну ціну (особливо після множення на коефіцієнт 0.95/0.9)?

---

*Документ підготовлено 27.08.2026 за детальним технічним завданням Кепа. Верхня частина — оригінальний текст без змін, нижня — структурована специфікація для реалізації та передачі прогеру.*
